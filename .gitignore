# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

audit-shots

# Session scratch work (temp scripts/data used while working, not part of the app)
.scratch/

# --- Backend -----------------------------------------------------------------
# .env me secret keys hoti hain (JWT_SECRET, SMTP password).
# Ise KABHI git par mat daalo.
server/.env

# Database. Yeh aapka asli data hai — iska backup alag se lo, git par nahi.
server/data/
